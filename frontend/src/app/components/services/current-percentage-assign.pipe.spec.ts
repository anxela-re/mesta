import { CurrentPercentageAssignPipe } from './current-percentage-assign.pipe';

describe('CurrentPercentageAssignPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentPercentageAssignPipe();
    expect(pipe).toBeTruthy();
  });
});
